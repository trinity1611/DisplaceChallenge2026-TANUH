## Medical Topic Identification (Track 3 Topic Identification)

This repository provides a **baseline implementation for Medical Topic Identification** from real-world healthcare conversations collected in rural India. The system is designed to process **multilingual conversational speech transcripts (Hindi, Hinglish, and code-mixed)** and extract the patient’s **current health problems** using a fine-tuned large language model.

The pipeline integrates:
- **LLM-based medical topic extraction (Qwen + LoRA)**
- **Hindi → English translation (LLaMA-based)**
- **Semantic evaluation using BERTScore**
- **Config-driven execution for reproducibility**

This work is inspired by the DISPLACE-M challenge and focuses on **semantic understanding of medical complaints**, rather than simple keyword extraction.

---

## Track Information

This repository is the official baseline for:

### **Track 3 – Topic Identification**

**Task:**  
Automatically identify and extract the **patient’s ongoing and current health problems** from healthcare conversations.

The output is a **comma-separated list of medical problem topics** for each conversation.

---

## 1. Repository Structure

```text
Track3_TI/
|
├── data/                           # Dataset directory 
├── config/
│   └── config.yaml                 # All model, path, and evaluation settings
│
├── src/
│   ├── utils/
│   │   ├── helpers.py
│   │   └── logger.py
│   │
│   ├── evaluator.py                # BERTScore-based semantic evaluation
│   ├── io_utils.py                 # CSV loading and saving
│   ├── llama_translator.py         # Hindi → English translation using LLaMA
│   ├── model.py                    # Qwen + LoRA model loader
│   ├── predictor.py                # Topic extraction prompt + inference
│   └── run_topic_inference.py      # Main pipeline runner
│
├── README.md
├── requirements.txt
```

## 2. Setup Instructions

### Step 1: 

```
# Clone Repository
git clone https://github.com/displace2026/DISPLACE-2026-Baselines.git
cd DISPLACE-2026-Baselines/Track3_TI
```

### Step 2:

- The TI baseline **requires Automatic Speaker Recognition (ASR) outputs** as input.
- Track1_SD and Track2_ASR needs to set-up and executed to get ASR output.
- Set the `wav` in `run.sh` of the Track1_SD system with the path of the Track3_TI audio files and set the `rttm` field as `None`.
- **Run Speaker Diarization baseline** by following the instructions in the README of Track1_SD. 
- Change the config file of the Track2_ASR system `input_audio_folder` field with the path of the Track4_DS audio files and set the `input_gt` field as `None`.
- **Run the ASR baseline** by following the instructions in the README of Track2_ASR. 
- Update the input path field `input_path` in TI configuration file ```config/config.yaml``` to point to the **output ASR transcripts generated by the ASR system**.

```
# To run Track 2 - ASR Pipeline
python ../Track2_ASR/scripts/asr.py
```

```
# Install Dependencies
# The same environment created for Track 2 - ASR will work for Track 3 - TI.
pip install -r requirements.txt
```

### Step 3:

```
# Set HuggingFace Token (For LLaMA Translation)

export HF_TOKEN=your_huggingface_token
```

Please follow the below instructins for Model Setup.

### Model Details
| Feature | Details |
| :--- | :--- |
| **Model Name** | `Llama-3.2-3B-Instruct` |
| **Repository** | [meta-llama/Llama-3.2-3B-Instruct](https://huggingface.co/meta-llama/Llama-3.2-3B-Instruct) |
| **Parameters** | 3B |
| **Reference** | [Dubey et al., 2024](https://arxiv.org/abs/2407.21783) |

### Model Setup
| Step | Action |
| :--- | :--- |
| **1. Request Access** | Go to the [Hugging Face model page](https://huggingface.co/meta-llama/Llama-3.2-3B-Instruct) and request access. |
| **2. Generate Token** | Generate a **Hugging Face Token** from your account settings. |
| **3. Configure** | Update the `hf_token` field in `configs/summarization.yaml` with your token. |

## 3. Infernece

To run the TI baselines please follow below steps. 

```
python src/run_topic_inference.py
```

> **Note:** The identified topics and corresponding metrics will be saved in ```outputs/Prediction_topic/``` and,  ```outputs/metrics/```

## 4. Evaluation Metrics

The following metrics will be used for evaluating Track 4 - Topic Identification task. 

### Extractive Score
In extractuve score, we are measuring the no. of topics correctly identified by using a similarity based Thresholding, and the similarity are being measured using ```all-MiniLM-L6-v2``` model.

- Precision

- Recall

- F1 Score

### BERTScore

Computes similarity using contextual embeddings (BERT), focusing on semantic meaning rather than exact word overlap.

## 5. Baseline Results
Baseline results on the development set:

**Records Processed:** 25

| Metric | Average Score |
| :--- | :--- |
| **Precision** | 0.545 |
| **Recall** | 0.396 |
| **F1-Score** | 0.459 |
| **Accuracy** | 0.43 |
| **BERTScore** | 0.962 |

> **Note:** These results serve as a reference. Participants are encouraged to improve upon them through model adaptation and decoding strategies.
